name: pokemon_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.4.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  http: ^0.13.3 # 依存関係にhttpを追加

dev_dependencies:
  flutter_test:
    sdk: flutter
  mockito: ^5.0.16 # モックライブラリ

flutter:
  uses-material-design: true
